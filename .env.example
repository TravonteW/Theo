# Copy to .env for local development. Do NOT commit .env.
# For Streamlit Community Cloud, set secrets instead (see README).
OPENAI_API_KEY=

# Optional: allow saving conversations.json to disk (NOT recommended for public deployments).
# THEO_PERSIST_CONVERSATIONS=1

# Optional: GPT-5 response controls (defaults: medium/medium).
# THEO_REASONING_EFFORT=medium
# THEO_TEXT_VERBOSITY=medium

# Optional: retrieval artifacts for deployments (if you don't commit index.faiss/citations.json).
# Provide either a zip bundle URL containing `index.faiss` and `citations.json`:
# THEO_ASSET_BUNDLE_URL=
# Or provide individual artifact URLs:
# THEO_INDEX_URL=
# THEO_CITATIONS_URL=
# Optional: where to store downloaded artifacts (defaults to a cache dir).
# THEO_ASSET_DIR=
